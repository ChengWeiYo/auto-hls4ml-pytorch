# auto-hls4ml-pytorch
A hls4ml repo supporting pytorch transformer&token reduction and automatically optimizing performance and resource utilization by setting hardware constraint and configuration.


```bash
git clone --recurse-submodules <repository-url>
git submodule update --init --recursive
```
